"""main.py - application entry point.

This is the command-line / programmatic entry point for the recruiting
chatbot. The full Main Agent orchestration (one conversation turn:
route to an advisor, consume its output, choose Continue / Schedule /
End) is implemented in later phases.

Phase 0: stub only, so the package imports cleanly.
"""


def main() -> None:
    """Run the chatbot entry point. Implemented in a later phase."""
    # TODO (Phase 6): wire up the Main Agent orchestration loop.
    print("GenAI Multi-Agent Recruiting Chatbot - not implemented yet.")


if __name__ == "__main__":
    main()
