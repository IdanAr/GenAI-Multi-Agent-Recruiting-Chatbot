"""app.prompts - version-controlled prompt text for each agent.

Per the project guidance, prompts (roles, instruction prompts, few-shot
examples) live here as named, version-controlled strings rather than
being buried inline in the agent code.

Phase 0: package placeholder. Populated as each agent is built.
"""
