# Keeps the data/ directory in version control.
# The Chroma store (data/chroma/) is git-ignored and rebuilt offline.
# The SQLite scheduling DB (data/tech.db) is tracked on purpose for reproducibility.
